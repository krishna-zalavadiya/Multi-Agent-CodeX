# ==============================================================================
# CodeSentinel — Python Dependencies
# Owner: Kishan (Backend Lead)
# Python version: 3.11
# Install: pip install -r requirements.txt
# ==============================================================================


# ------------------------------------------------------------------------------
# Web Framework
# ------------------------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server with websocket + reload support


# ------------------------------------------------------------------------------
# Database
# ------------------------------------------------------------------------------
sqlalchemy==2.0.30                 # Async ORM
asyncpg==0.29.0                    # Async PostgreSQL driver (required for +asyncpg URL)
aiosqlite==0.20.0                  # Async SQLite driver — used in pytest only


# ------------------------------------------------------------------------------
# Config / Settings
# ------------------------------------------------------------------------------
pydantic==2.7.1
pydantic-settings==2.2.1          # BaseSettings — reads from .env file


# ------------------------------------------------------------------------------
# Task Queue
# ------------------------------------------------------------------------------
celery==5.4.0                      # Distributed task queue
redis==5.0.4                       # Redis client — Celery broker + backend
flower==2.0.1                      # Celery monitoring dashboard


# ------------------------------------------------------------------------------
# HTTP Client — used by Naivadh's OllamaClient
# ------------------------------------------------------------------------------
httpx==0.27.0                      # Async HTTP client with timeout support


# ------------------------------------------------------------------------------
# AI / Orchestration — Naivadh's stack (pinned for compatibility)
# ------------------------------------------------------------------------------
langgraph==0.1.5                   # LangGraph multi-agent orchestration
langchain-core==0.2.5              # LangChain core (required by LangGraph)
ollama==0.2.1                      # Ollama Python client


# ------------------------------------------------------------------------------
# Utilities
# ------------------------------------------------------------------------------
python-multipart==0.0.9            # Required for FastAPI file/form uploads
python-dotenv==1.0.1               # Loads .env file (backup to pydantic-settings)
orjson==3.10.3                     # Fast JSON serialization for FastAPI responses


# ------------------------------------------------------------------------------
# Testing
# ------------------------------------------------------------------------------
pytest==8.2.0
pytest-asyncio==0.23.6             # Async test support
pytest-cov==5.0.0                  # Coverage reporting
httpx==0.27.0                      # Also used as AsyncClient in API tests


# ------------------------------------------------------------------------------
# Code Quality (optional but recommended)
# ------------------------------------------------------------------------------
ruff==0.4.4                        # Fast linter + formatter